{
  "name": "betfair-automation",
  "version": "0.1.0",
  "private": true,
  "description": "Runs on the Mac (not Render) -- drives a real, persistent Chrome profile against Betfair Sportsbook using betfairPlan.js's ordered plan, with Stagehand as a narrow AI fallback only where deterministic Playwright locators fail. See README.md before running this anywhere.",
  "type": "module",
  "main": "driver.js",
  "scripts": {
    "place": "node driver.js"
  },
  "dependencies": {
    "@browserbasehq/stagehand": "^2.0.0",
    "playwright": "^1.47.0"
  }
}
